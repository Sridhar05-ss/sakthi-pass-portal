import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sakthipassportal',
  appName: ' Sakthi Pass Porta',
  webDir: 'dist'
};

export default config;
